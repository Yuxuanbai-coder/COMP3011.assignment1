/**
 * Services containing the application's business logic and process-wide
 * coordination state.
 *
 * <p>Services are deliberately kept separate from HTTP controllers so the
 * same logic can be tested without a browser or a live Cloud API.</p>
 */
package comp3011.assignment.service;
